services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - pocket_trails_data:/app/data
    environment:
      DATABASE_URL: sqlite:////app/data/pocket_trails.db

volumes:
  pocket_trails_data:
